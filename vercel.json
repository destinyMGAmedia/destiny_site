{
  "framework": "nextjs",
  "buildCommand": "prisma db push && npx next build",
  "functions": {
    "src/app/**/*.{js,jsx}": {
      "maxDuration": 30
    }
  },
  "build": {
    "env": {
      "SKIP_ENV_VALIDATION": "1"
    }
  }
}
